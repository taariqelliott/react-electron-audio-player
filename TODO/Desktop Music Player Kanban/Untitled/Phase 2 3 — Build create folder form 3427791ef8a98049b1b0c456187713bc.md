# Phase 2.3 — Build create folder form

Status: Done

Use shadcn `Dialog` triggered by a button in the sidebar. Inside the dialog use `Input` for name and artist, `Select` for type (album, playlist, folder). On submit send data to main via IPC.