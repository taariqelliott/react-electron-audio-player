# Phase 7.2 — Search query — tracks and folders

Status: To Do

Main receives the query string and runs a JOIN across `folders` and `tracks` matching on title, artist, and folder name. Returns results with `folderPath` and filename.