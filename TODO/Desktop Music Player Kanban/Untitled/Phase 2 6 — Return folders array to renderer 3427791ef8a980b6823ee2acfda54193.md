# Phase 2.6 — Return folders array to renderer

Status: Done

After scanning attach `folderPath` to each manifest object and return the full array to the renderer via IPC. Renderer stores it in state.