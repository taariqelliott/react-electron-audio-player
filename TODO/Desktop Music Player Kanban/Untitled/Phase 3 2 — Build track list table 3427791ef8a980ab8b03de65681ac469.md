# Phase 3.2 — Build track list table

Status: In progress

— Build track list table

Below the folder header render the `tracks` array sorted by `trackOrder` using shadcn `Table` with columns for order, title, artist, and duration.