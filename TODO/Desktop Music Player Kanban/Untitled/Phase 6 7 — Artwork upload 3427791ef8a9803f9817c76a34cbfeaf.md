# Phase 6.7 — Artwork upload

Status: To Do

Inside the folder edit `Dialog` add an image picker button. Main copies the selected image into the folder directory as `artwork.jpg` overwriting any existing one. Renderer updates the `file://` URL displayed in the sidebar and folder header.