# Phase 6.1 — Edit track title and artist inline

Status: To Do

Clicking a track row field in the `Table` switches it to an editable shadcn `Input`. On blur or enter send the updated fields to main via IPC. Main updates the manifest entry.