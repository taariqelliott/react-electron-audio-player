# Phase 2.7 — Build sidebar with folder list

Status: Done

Use shadcn `Sidebar` as the main navigation container. Each folder renders as a `SidebarMenuItem` showing folder name and type. Clicking a folder sets `activeFolder` in state.