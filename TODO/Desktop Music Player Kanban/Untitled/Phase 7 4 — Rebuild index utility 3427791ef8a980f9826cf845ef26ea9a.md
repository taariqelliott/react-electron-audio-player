# Phase 7.4 — Rebuild index utility

Status: To Do

Wipe both tables and rescan every manifest in `libraryRoot` to repopulate SQLite from scratch. Run automatically on first launch if database is empty. Expose as a manual button in settings.