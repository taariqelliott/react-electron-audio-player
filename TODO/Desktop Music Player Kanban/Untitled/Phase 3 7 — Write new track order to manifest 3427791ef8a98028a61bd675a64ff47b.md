# Phase 3.7 — Write new track order to manifest

Status: To Do

After drag reorder send the new ordered array to main via IPC. Main reassigns `trackOrder` values sequentially and writes manifest back.