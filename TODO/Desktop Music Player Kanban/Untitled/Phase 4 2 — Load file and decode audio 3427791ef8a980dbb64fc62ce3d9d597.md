# Phase 4.2 — Load file and decode audio

Status: Done

Read directly from the `File` object via `arrayBuffer()`. Pass raw bytes to `decodeAudioData` to get an `AudioBuffer`. Store the decoded buffer in a ref.