# Phase 1.1 — Scaffold Electron + Vite + React

Status: Done

Set up the project using `electron-vite` which handles the main/renderer/preload split out of the box. Three folders: `main` (Node.js, fs, SQLite), `preload` (the bridge), `renderer` (React app).