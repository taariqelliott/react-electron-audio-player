# Phase 1.8 — Show setup screen when libraryRoot is null

Status: Done

If `libraryRoot` is null after reading config show a setup screen with a button to trigger the folder picker. Use shadcn `Card` to contain it.