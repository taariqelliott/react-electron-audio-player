# Phase 3.3 — Append tracks to manifest

Status: To Do

After copying main reads the manifest, appends a new track entry for each file with filename, title defaulting to filename, empty artist, order, and addedAt timestamp. Updates `totalTracks`. Writes manifest back.