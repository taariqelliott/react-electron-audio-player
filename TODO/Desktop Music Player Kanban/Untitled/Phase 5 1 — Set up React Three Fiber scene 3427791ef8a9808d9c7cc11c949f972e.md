# Phase 5.1 — Set up React Three Fiber scene

Status: To Do

Install `@react-three/fiber` and `@react-three/drei`. Set up a Canvas component alongside the player UI. Pass the `AnalyserNode` into the scene via React context.