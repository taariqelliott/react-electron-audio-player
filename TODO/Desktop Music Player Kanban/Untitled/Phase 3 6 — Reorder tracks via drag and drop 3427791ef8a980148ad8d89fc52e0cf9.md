# Phase 3.6 — Reorder tracks via drag and drop

Status: To Do

Use `@dnd-kit` for sortable drag and drop on the track `Table` rows. Each row has a drag handle on the left. React updates state immediately for instant feel.