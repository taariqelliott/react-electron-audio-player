# Phase 3.5 — Return updated track list to renderer

Status: To Do

Main returns the updated manifest to the renderer. Renderer updates `activeFolder` state so the track list re-renders with the new tracks.