# Phase 7.1 — Build search input

Status: To Do

Use shadcn `Command` component in the sidebar header. Handles input, results list, and keyboard navigation out of the box. Debounce input at 200ms before querying.